import {
  countCarrigeReturn,
  hasProperty,
  isFunction,
  mergeObjects,
  safeGet
} from './general';
import * as Placeholder from './placeholder';
import * as DomNode from './domNode';
import * as Tokens from './tokens';


export {
  // General
  countCarrigeReturn,
  hasProperty,
  isFunction,
  mergeObjects,
  safeGet,
  // Interfaces & Helpers
  DomNode,
  Placeholder,
  Tokens
};
